tool_id: defi.active_loans_per_token
g1: defi
description: Aggregates active loan IDs concatenated per borrow token.
g2: lending
g3: active_loans_per_token
g4: Active Loans
g5: loopscale instructions
scope: protocol:loopscale
returns:
- name: borrow_token
  type: string
- name: loan_ids
  type: string
inputs:
- key: date_from
  type: date
  label: Start Date
  required: true
