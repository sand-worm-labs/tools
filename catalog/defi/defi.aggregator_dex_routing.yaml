tool_id: defi.aggregator_dex_routing
g1: defi
description: Detects transactions where aggregator routes to specific DEX via inner
  instructions.
g2: dex
g3: aggregator_dex_routing
g4: Aggregator DEX Flows
g5: Solana Transactions
scope: generic
returns:
- name: day
  type: date
- name: user
  type: string
- name: tx_id
  type: string
inputs:
- key: aggregator_address
  type: address
  label: Aggregator Address
  required: true
- key: dex_programs
  type: text
  label: DEX Programs
  required: true
- key: date_from
  type: date
  label: Start Date
  required: true
