tool_id: defi.dex_user_protocol_overlap
g1: defi
g2: dex
g3: dex_user_protocol_overlap
g4: User Overlap %
g5: Cross-DEX cohort join
description: Computes overlap of users between source and target DEX, flagging prior experience.
scope: generic
returns:
- name: overlap_pct
  type: number
- name: source_users
  type: number
- name: target_users
  type: number
inputs:
- key: source_protocol
  label: Source DEX
  type: text
  required: true
- key: target_protocol
  label: Target DEX
  type: text
  required: true
- key: date_from
  label: Start Date
  type: date
  required: true
