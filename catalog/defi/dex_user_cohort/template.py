params = {
    "chain": "{{chain}}",
    "protocol": "{{protocol}}",
    "contract_address": "{{contract_address}}",
    "interval": "{{interval}}",
    "lookback_days": "{{lookback_days}}",
}
print(params)
