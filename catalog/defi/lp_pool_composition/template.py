params = {
    "pool_address": "{{pool_address}}",
    "token1_address": "{{token1_address}}",
    "token2_address": "{{token2_address}}",
    "decimals1": "{{decimals1}}",
    "decimals2": "{{decimals2}}",
}
print(params)
