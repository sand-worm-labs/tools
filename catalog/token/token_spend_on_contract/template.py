params = {
    "chain": "{{chain}}",
    "token_contract": "{{token_contract}}",
    "target_contract": "{{target_contract}}",
    "topic0_transfer": "{{topic0_transfer}}",
    "date_to": "{{date_to}}",
}
print(params)
